<li data-id="{{_id}}">
    <div class="cell small-3" data-id="{{_id}}">
        Code: {{colorCode}}
        Quantity: {{weightIn}}
    </div>
    <div class="button" data-id="{{_id}}">Add</div>
    <div class="button" data-id="{{_id}}">Remove</div>
</li>