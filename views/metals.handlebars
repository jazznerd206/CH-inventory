{{>navbar}}

<div class="container">
  <div class="title-container">
    <h1>METALS</h1>
  </div>

      <a href="#" class="title">
          <div class="button cell-small-12" onclick="window.location.href = '/metal/other';">
              TOTALS
          </div>
          
      </a>
      <div class="cell small-12 table" id="reichenbachBarDataHolder">
        <ul style="list-style: none;">
          {{#if isLoggedIn}}
          {{#each metals}}
              {{> colorListItemWithButtons}}
          {{/each}}
          {{else}}
          {{#each metals}}
              {{> colorListItemWithoutButtons}}
          {{/each}}
          {{/if}}

        </ul>
      </div>        
</div>